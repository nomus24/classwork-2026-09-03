# 【仕様設計書】

- 学籍番号：nk210001
- 氏名：野村颯摩
- 提出日時：2026年09月03日 15:39

## ① コマンド一覧

- コマンド：`python main.py add --title <タイトル> [--author <著者名>]`　説明：蔵書を1冊登録し、`add_book()`を実行する。　入力：タイトル（必須）、著者名（任意）　出力：登録した本の管理ID、タイトル、著者名
- コマンド：`python main.py list [--status available|lent|overdue]`　説明：蔵書を一覧表示し、状態を指定した場合は絞り込む。`list_books()`を実行する。　入力：状態（任意）　出力：管理ID、タイトル、著者名、現在の状態の一覧
- コマンド：`python main.py search <キーワード>`　説明：タイトルまたは著者名を部分一致で検索し、`search_books()`を実行する。　入力：検索キーワード　出力：該当する本の管理ID、タイトル、著者名、現在の状態
- コマンド：`python main.py count`　説明：状態別の冊数を集計し、`count_books()`を実行する。　入力：なし　出力：総数、貸出可能、貸出中、延滞中の各冊数
- コマンド：`python main.py lend <管理ID> --to <貸出相手> [--lent-on <貸出日>] [--due-on <返却期限>]`　説明：貸出可能な本の貸出記録を作成し、`lend_book()`を実行する。　入力：管理ID、貸出相手、貸出日（省略時は当日）、返却期限（省略時は貸出日の14日後）　出力：本のタイトル、貸出相手、貸出日、返却期限
- コマンド：`python main.py loans [--all]`　説明：貸出状況を表示し、`list_loans()`を実行する。通常は未返却分だけ、`--all`指定時は返却済みを含む履歴を表示する。　入力：全履歴表示の指定（任意）　出力：貸出ID、管理ID、タイトル、貸出相手、貸出日、返却期限、返却日、状態
- コマンド：`python main.py return <管理ID> [--returned-on <返却日>]`　説明：対象の未返却貸出に返却日を記録し、`return_book()`を実行する。　入力：管理ID、返却日（省略時は当日）　出力：返却した本のタイトルと返却日
- コマンド：`python main.py edit <管理ID> [--title <タイトル>] [--author <著者名>]`　説明：蔵書情報を修正し、`edit_book()`を実行する。　入力：管理ID、変更するタイトルまたは著者名　出力：変更後の管理ID、タイトル、著者名
- コマンド：`python main.py delete <管理ID>`　説明：貸出中でない蔵書を論理削除し、通常の一覧から除外する。`delete_book()`を実行する。　入力：管理ID　出力：削除した本の管理IDとタイトル
- コマンド：`python main.py help [コマンド名]`　説明：`show_help()`を実行し、全体または指定コマンドの使い方を表示する。　入力：コマンド名（任意）　出力：コマンドの説明、引数、実行例

## ② コマンドフロー図

- 共通フロー：コマンドと引数を受け取る → コマンド名と入力形式を確認する → 対応する関数を呼び出す → SQLiteへ接続する → 処理結果またはエラーを日本語で表示する → 接続を閉じる
- 蔵書登録（`add`）：タイトルと著者名を受け取る → タイトルが空でないか確認する → `books`テーブルへ追加する → トランザクションを確定する → 発行された管理IDと登録内容を表示する
- 貸出（`lend`）：管理IDで有効な本を検索する → その本に未返却の貸出記録がないか確認する → 貸出相手と日付を検証する → トランザクションを開始する → `loans`テーブルへ未返却の貸出記録を追加する → トランザクションを確定する → 貸出内容を表示する
- 返却（`return`）：管理IDで本と未返却の貸出記録を検索する → 返却日を検証する → トランザクションを開始する → 対象の`loans.returned_on`を更新する → トランザクションを確定する → 返却内容を表示する。未返却記録がなくなるため、その本は自動的に貸出可能になる
- 蔵書編集（`edit`）：管理IDで有効な本を検索する → 変更項目を検証する → `books`テーブルを更新する → トランザクションを確定する → 変更後の内容を表示する
- 蔵書削除（`delete`）：管理IDで有効な本を検索する → 未返却の貸出記録がないか確認する → `books.is_active`を`0`へ更新する → トランザクションを確定する → 削除結果を表示する。貸出履歴を残すため、データ自体は物理削除しない
- 一覧・検索・集計（`list`、`search`、`count`）：条件を検証する → `books`と未返却の`loans`を参照する → 状態を判定する → 条件に合うデータを整形して表示する。この処理ではデータを変更しない

状態はデータの重複を避けるため専用カラムには保存せず、次の規則で表示時に判定する。

- 貸出可能：対象の本に`returned_on IS NULL`の貸出記録がない
- 貸出中：未返却の貸出記録があり、現在日が返却期限以前
- 延滞中：未返却の貸出記録があり、現在日が返却期限を過ぎている

## ③ データベース設計

データベースにはSQLiteを使用し、日付は`YYYY-MM-DD`、日時はISO 8601形式の文字列として保存する。

### テーブル名：`books`（蔵書）

- カラム名：`id`　型：INTEGER　説明：管理ID。主キー、自動採番
- カラム名：`title`　型：TEXT　説明：本のタイトル。必須
- カラム名：`author`　型：TEXT　説明：著者名。任意のためNULL可
- カラム名：`is_active`　型：INTEGER　説明：有効なら`1`、論理削除済みなら`0`。必須、初期値`1`
- カラム名：`created_at`　型：TEXT　説明：登録日時。必須
- カラム名：`updated_at`　型：TEXT　説明：最終更新日時。必須

### テーブル名：`loans`（貸出履歴）

- カラム名：`id`　型：INTEGER　説明：貸出ID。主キー、自動採番
- カラム名：`book_id`　型：INTEGER　説明：貸した本の管理ID。必須、`books.id`を参照する外部キー
- カラム名：`borrower_name`　型：TEXT　説明：貸出相手の名前。必須
- カラム名：`lent_on`　型：TEXT　説明：貸出日。必須、`YYYY-MM-DD`形式
- カラム名：`due_on`　型：TEXT　説明：返却期限。必須、貸出日以降の日付
- カラム名：`returned_on`　型：TEXT　説明：実際の返却日。未返却の場合はNULL、返却済みの場合は貸出日以降の日付
- カラム名：`created_at`　型：TEXT　説明：貸出記録の作成日時。必須

### テーブル間の関係

- `books`の1件に対して`loans`は0件以上存在する「1対多」の関係とする。1冊の本は何度も貸し出せるため、過去の貸出を上書きせず、貸すたびに新しい`loans`行を追加する。
- `loans.book_id`から`books.id`を外部キーで参照し、存在しない本に貸出記録を作れないようにする。
- 1冊に未返却の貸出を複数作れないよう、`loans(book_id)`に「`returned_on IS NULL`の行だけを対象とする一意インデックス」を設定する。
- 貸出履歴と外部キーの関係を壊さないため、`delete`では`books`を物理削除せず、`is_active = 0`にする。

## ④ エラー処理の一覧

- 状況：存在しないコマンドが入力された　メッセージ例：`エラー：コマンドが見つかりません。使い方は「python main.py help」で確認してください。`
- 状況：必須の引数が指定されていない　メッセージ例：`エラー：--title は必須です。`
- 状況：タイトルが空文字または空白だけである　メッセージ例：`エラー：タイトルを入力してください。`
- 状況：検索キーワードが空である　メッセージ例：`エラー：検索キーワードを入力してください。`
- 状況：管理IDが整数でない　メッセージ例：`エラー：管理IDは整数で指定してください。`
- 状況：指定した管理IDの本が存在しない、または削除済みである　メッセージ例：`エラー：管理ID 12 の本は見つかりません。`
- 状況：貸出相手が空文字または空白だけである　メッセージ例：`エラー：貸出相手を入力してください。`
- 状況：日付の形式が正しくない、または存在しない日付である　メッセージ例：`エラー：日付は YYYY-MM-DD 形式で入力してください。例：2026-09-03`
- 状況：返却期限が貸出日より前である　メッセージ例：`エラー：返却期限は貸出日以降の日付にしてください。`
- 状況：すでに貸出中の本を再度貸し出そうとした　メッセージ例：`エラー：管理ID 12 の本はすでに貸出中です。`
- 状況：貸出中でない本を返却しようとした　メッセージ例：`エラー：管理ID 12 の本には未返却の貸出記録がありません。`
- 状況：返却日が貸出日より前、または現在より後である　メッセージ例：`エラー：返却日は貸出日から本日までの範囲で指定してください。`
- 状況：`edit`で変更項目が一つも指定されていない　メッセージ例：`エラー：--title または --author の少なくとも一つを指定してください。`
- 状況：貸出中の本を削除しようとした　メッセージ例：`エラー：貸出中の本は削除できません。返却処理を先に行ってください。`
- 状況：状態の絞り込み値が正しくない　メッセージ例：`エラー：--status は available、lent、overdue のいずれかを指定してください。`
- 状況：一覧または検索に該当データがない　メッセージ例：`該当する本はありません。`
- 状況：データベースの読み書きに失敗した　メッセージ例：`エラー：データベースを更新できませんでした。変更は保存されていません。`

入力エラーではデータベースを更新しない。更新途中で例外が発生した場合はトランザクションをロールバックし、途中までの変更が残らないようにする。
