# 【要件定義書】

- 学籍番号：nk210001
- 氏名：野村颯摩
- 提出日時：2026年09月03日 14:52

## ① 利用者像

このツールを使うのは、自宅に本を複数所有し、ときどき友人へ貸している個人である。自分のパソコン上で、蔵書の登録や確認、貸出・返却の記録を簡単な日本語のコマンド操作で行う場面を想定する。

## ② 困りごと（現状の課題）

所有している本の情報が一か所に整理されていないため、本棚に全部で何冊あるのか、どの本が手元にあるのかをすぐに把握できない。また、友人へ貸した際の記録が残っていないため、誰にどの本をいつ貸したのかを忘れ、返却のお願いができないことがある。その結果、本が長期間戻らない、または紛失したことにも気づきにくい。

## ③ ユースケース一覧

- 所有している本を1冊ずつ登録する。
- 登録済みの本を一覧で確認する。
- タイトルや著者名から本を検索する。
- 蔵書の総数、手元にある冊数、貸出中の冊数を確認する。
- 手元にある本を選び、貸した相手と貸出日を記録する。
- 貸出時に返却期限を設定する。
- 貸出中の本について、貸出先と返却期限を一覧で確認する。
- 返却期限を過ぎた本を確認する。
- 本が返却されたことを記録し、再び貸出可能な状態に戻す。
- 本の情報を修正する。
- 不要になった本の登録を削除する。

## ④ 機能要求（MUST）

- 蔵書登録機能：タイトルを必須、著者名を任意として本を1冊ずつ登録し、各冊に重複しない管理IDを付ける。
- 蔵書一覧機能：登録した本について、管理ID、タイトル、著者名、現在の状態を一覧表示する。
- 検索機能：タイトルまたは著者名の一部を指定して、該当する本を検索できるようにする。
- 冊数集計機能：蔵書の総数、貸出可能な冊数、貸出中の冊数、延滞中の冊数を表示する。
- 貸出登録機能：貸出可能な本を管理IDで選択し、貸出相手、貸出日、返却期限を記録する。返却期限は貸出日の14日後を初期値とする。
- 貸出状況確認機能：貸出中の本について、タイトル、貸出相手、貸出日、返却期限、延滞の有無を一覧表示する。
- 延滞判定機能：未返却のまま現在日が返却期限を過ぎた本を、自動的に「延滞」として表示する。
- 返却処理機能：貸出中の本を管理IDで選択して返却日を記録し、その本を再び貸出可能にする。
- 蔵書編集機能：管理IDを指定して、登録済みのタイトルまたは著者名を修正する。
- 蔵書削除機能：管理IDを指定して蔵書を削除する。ただし、貸出中の本は返却処理が完了するまで削除できない。
- 入力チェック機能：必須項目の未入力、存在しない管理ID、貸出中の本の二重貸出、不正な日付を検出し、理由が分かるエラーメッセージを表示する。
- データ保存機能：蔵書と貸出のデータをローカルのSQLiteデータベースファイル（`bookshelf.db`）へ保存し、ツールを終了しても次回起動時に引き継ぐ。

## ⑤ 非機能要求

- 操作性：画面表示とエラーメッセージは日本語とし、各操作で何を入力すればよいか分かるようにする。
- 確認しやすさ：一覧は見出しを付けて整列し、貸出中・延滞中の状態を区別して表示する。
- データ永続性：状態を変更する操作はSQLiteのトランザクション内で行い、正常終了時に`bookshelf.db`へ確定する。再起動後も記録が残るようにする。
- データ整合性：各冊を管理IDで識別し、同じ本の二重貸出や貸出中データの誤削除を防ぐ。
- 安全性：保存前に入力内容を検証し、エラー時には既存データを変更しない。
- プライバシー：貸出相手の名前を含むデータは外部サービスへ送信せず、利用者のパソコン内だけに保存する。
- 性能：蔵書が1,000冊程度あっても、通常の登録・検索・一覧表示を1秒程度で完了できることを目標とする。

## ⑥ 意思決定と理由

- 返却期限は何日後にするか、その根拠：原則として貸出日の14日後とする。短すぎると相手が本を読む時間を確保しにくく、長すぎると貸した側が忘れやすいため、2週間を双方にとって扱いやすい期間と判断した。ただし、事情に合わせて貸出時に別の日付へ変更できるようにする。
- 返却期限を過ぎた本（延滞）をどう扱うか：データを削除したり罰則を設けたりせず、「延滞」として目立つように表示し、延滞一覧で貸出相手と期限を確認できるようにする。催促するかどうかは人間関係にも関わるため、ツールは判断せず、利用者が連絡するための情報提供にとどめる。
- 同じタイトルの本を複数冊登録できるようにするか：登録できるようにする。同じ本を保存用と貸出用で複数持つ場合や、版の違う本を持つ場合があるためである。タイトルではなく、登録時に発行する管理IDで1冊ずつ区別し、それぞれの貸出状態を管理する。
